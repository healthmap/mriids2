import React from "react";
import { shallow } from "enzyme";
import { TimespanButtonsWrapper } from "../TimespanButtonsWrapper";

test("render TimespanButtonsWrapper", () => {
  shallow(<TimespanButtonsWrapper />);
});
