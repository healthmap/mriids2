import React from "react";
import { shallow } from "enzyme";
import { ChartTypeButtonsWrapper } from "../ChartTypeButtonsWrapper";

test("render ChartTypeButtonsWrapper", () => {
  shallow(<ChartTypeButtonsWrapper />);
});
