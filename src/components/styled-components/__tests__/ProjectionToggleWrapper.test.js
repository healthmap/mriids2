import React from "react";
import { shallow } from "enzyme";
import { ProjectionToggleWrapper } from "../ProjectionToggleWrapper";

test("render ProjectionToggleWrapper", () => {
  shallow(<ProjectionToggleWrapper />);
});
