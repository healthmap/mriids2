import styled from "styled-components";

export const CaseCountToggleWrapper = styled.div`
  position: absolute;
  bottom: 1rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 1em;
  padding: 1em 0.5em 0 0.5em;
  border-top: 1px solid #ccc;
  cursor: pointer;
  label {
    margin-left: 0.8em;
    line-height: 1.1;
    color: #666;
    cursor: pointer;
  }
`;
